{
  "name": "schema",
  "private": true,
  "description": "Schema, database structure for entire app",
  "sideEffects": false,
  "type": "module",
  "main": "./build/index.js",
  "module": "./build/index.js",
  "types": "./index.ts",
  "scripts": {
    "dev": "bun build ./index.ts --outfile build/index.js --no-bundle --watch",
    "build": "bun build ./index.ts --outfile build/index.js --no-bundle",
    "seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "5.7.1",
    "@prisma/extension-read-replicas": "0.3.0"
  },
  "devDependencies": {
    "@types/bun": "1.0.0",
    "eslint": "8.55.0",
    "eslint-plugin-config": "workspace:*"
  }
}
